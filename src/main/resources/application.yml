#spring:
#  datasource:
#    url: jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
#    username: ${POSTGRES_USER}
#    password: ${POSTGRES_PASSWORD}
#    driver-class-name: org.postgresql.Driver

#  liquibase:
#    enabled: false
#    properties:
#      hibernate:
#        format_sql: true
spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/intershop
    username: intershop1
    password: admin123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  liquibase:
    change-log: classpath:/db/changelog/db.changelog-master.xml
    enabled: true

logging:
  level:
    root: INFO
    ru.rpovetkin: DEBUG